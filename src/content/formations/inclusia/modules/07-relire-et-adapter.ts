import type { Module } from "@/content/types";

/** Module provisoire — contenu en cours de rédaction. */
export const relireEtAdapter: Module = {
  slug: "relire-et-adapter",
  numero: 7,
  titre: "Relire, ajuster, exporter",
  sousTitre: "",
  duree: 15,
  objectif: "",
  blocs: [],
};
